.DS_Store
/credentials/
# .NET build artifacts
src/**/bin/
src/**/obj/

# User secrets
src/**/appsettings.Development.json
**/secrets.json

# Test config con credenciales reales (la plantilla sin secretos sí se versiona)
src/**/appsettings.test.json

# Docker env vars con secretos reales (el .example sí se versiona)
.env.docker
